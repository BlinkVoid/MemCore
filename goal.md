let's make a plan for a memory management system, previously we've been doing  short-term / long term memory via some isolated attempts, but have not really thought through for a well thought memory management for AI across different repos, systems, tools. what we want is a standalone memory system which can use either local LLM or cloud services (via API integration with kimi K2.5 or bedrock or deepseek), the AI agent is a watched process using strand sdk, acting as the gate keeper with a set of mcp tools to itself, it can then consolidate memories and respond to external mcp client request to retrive memory and provide context. we will build out the architecture and further requirements later. first. make a plan document under docs folder within the root to document the initial goal, and proceed to conduct deep research and arxiv researches to find avaiable methdologies and techniques about AI memory management, and there's a open source package to openviking by byte dance. once you've doncuted the research, document that agent-memory-management-research.md, then we can discuss on the research. 